<div class="chat-page">

  <div class="chat-header">
    <%= link_to "← Mes conversations", chats_path, class: "back-link" %>

    <div class="chat-title-block">
      <span class="chat-subtitle">Conversation</span>
      <h1><%= @chat.title %></h1>
    </div>
  </div>

  <% if @chat.cocktail.present? %>
    <div class="cocktail-recommendation">
      <div class="cocktail-badge">
        🍸 Bartender's Choice
      </div>

      <h2><%= @chat.cocktail.name %></h2>

      <div class="cocktail-section">
        <strong>Ingredients</strong>
        <p><%= @chat.cocktail.ingredients %></p>
      </div>

      <div class="cocktail-section">
        <strong>Recipe</strong>
        <p><%= @chat.cocktail.recipe %></p>
      </div>
    </div>
  <% end %>

  <div class="messages-container">

    <div id="messages" class="messages-container">
      <% if @chat.messages.any? %>
        <%= render "messages/messages", messages: @chat.messages %>
      <% else %>
        <div class="message-row assistant-row">
          <div class="message-bubble assistant-bubble">
            <div class="message-role">The Bartender</div>
            <p>Bonjour, qu'est-ce que je peux faire pour vous ?</p>
          </div>
        </div>
      <% end %>
    </div>

    <div class="chat-input-container">
      <%= render "messages/form", chat: @chat, message: @message %>
    </div>
      </div>
  </div>
